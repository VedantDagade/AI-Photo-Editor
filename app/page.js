import { Button } from "@/components/ui/button";
import Image from "next/image";

export default function Home() {
  return (
    <div>
      Vedant Dagade <br />
      <Button variant="primary">Start app</Button>
      <Button variant="glass">Start</Button>
    </div>
  );
}
